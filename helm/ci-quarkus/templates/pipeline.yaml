apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: quarkus-ci
  namespace: {{ .Values.namespace }}
spec:
  params:
    - name: APP_REPO
      default: {{ .Values.appRepo | quote }}
    - name: APP_REVISION
      default: {{ .Values.appRevision | quote }}
    - name: IMAGE_REPO
      default: {{ .Values.imageRepo | quote }}
    - name: GITOPS_REPO
      default: {{ .Values.gitopsRepo | quote }}
    - name: CHART_PATH
      default: {{ .Values.chartPath | quote }}
  workspaces:
    - name: shared
    - name: gitops-creds
    - name: registry-auth
  tasks:
    - name: clone
      resolver: cluster
      taskRef:
        name: git-clone
        resolver: cluster
        params:
          - name: kind
            value: task
          - name: namespace
            value: openshift-pipelines
      params:
        - name: URL
          value: $(params.APP_REPO)
        - name: REVISION
          value: $(params.APP_REVISION)
      workspaces:
        - name: output
          workspace: shared

    - name: mvn-build
      runAfter: [clone]
      taskRef:
        name: jib-maven
        resolver: cluster
        params:
          - name: kind
            value: task
          - name: namespace
            value: openshift-pipelines
      params:
        - name: GOALS
          value:
            - -DskipTests
            - package
        - name: IMAGE
          value: quarkus-dev
      workspaces:
        - name: source
          workspace: shared
        - name: maven-settings
          workspace: shared

    - name: build-push
      runAfter: [mvn-build]
      taskRef:
        name: buildah
        resolver: cluster
        params:
          - name: kind
            value: task
          - name: namespace
            value: openshift-pipelines
      params:
        - name: IMAGE
          value: $(params.IMAGE_REPO):$(tasks.clone.results.commit)
        - name: DOCKERFILE
          value: ./Dockerfile
        - name: CONTEXT
          value: .
        - name: TLSVERIFY
          value: {{ .Values.tlsVerify | quote }}
      workspaces:
        - name: source
          workspace: shared
        - name: dockerconfig
          workspace: registry-auth